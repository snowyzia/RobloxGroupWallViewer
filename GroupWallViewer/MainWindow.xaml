<Window x:Class="GroupWallViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GroupWallViewer"
        xmlns:userControls="clr-namespace:GroupWallViewer.View.UserControls"
        mc:Ignorable="d"
        Title="Group Viewer" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="80"/>
            <RowDefinition/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <Menu Background="#E3E3E3">
            <MenuItem Header="File" FontSize="16">
                <MenuItem Header="Open Json" Click="OpenJson"/>
                <MenuItem Name="clearCache" Header="Clear Icon Cache" Visibility="Visible" Click="ClearCache"/>
                <MenuItem Header="Close Group" Click="CloseGroup"/>
                <MenuItem Header="Exit" Click="ExitClick"/>
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1" Background="#F0F0F0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Image Name="groupIcon" Margin="5"/>

            <Grid Grid.Column="1" Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                
                <TextBlock Name="groupName" Text="" VerticalAlignment="Center"/>
                <TextBlock Name="groupDescription" Text="" Grid.Row="1" VerticalAlignment="Center"/>
            </Grid>
        </Grid>

        <ScrollViewer Grid.Row="2" VirtualizingPanel.ScrollUnit="Pixel">
            <StackPanel Name = "groupHolder">
  
            </StackPanel>
        </ScrollViewer>
        
        <TextBlock Name = "infoText" Text="Waiting on group data..." Grid.Row="32" Margin="2"/>
    </Grid>
</Window>
